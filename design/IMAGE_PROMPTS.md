# Molife image prompt log

All visual boards in this folder were generated with the in-app Image generation tool. The two user-supplied images were used as style references only.

## Avatar system v0.1

Create a polished landscape character-system board for Molife, a warm adult workplace product about finishing the workday and returning to a fuller life. Use the supplied portrait sheet only for its chunky silhouette, simple facial marks, playful asymmetry, flat painted shapes, and hand-drawn energy; do not copy any character. Use the existing Molife UI map for the cream paper, deep teal, sunset orange, cornflower blue, gouache/crayon grain, and optimistic after-work mood. Show eight distinct adult office-worker portraits, one anchor character with four consistent expressions, and modular hair, glasses, accessories, life objects, and background colors. Keep the result mature, diverse, inclusive, lightly humorous, and production-ready. Avoid baby faces, mascot animals, anime, glossy 3D, photorealism, and corporate stock-vector styling.

## Ticket system v0.1

Create an original `MOLIFE 生活车票系统 v0.1` design board. Use the supplied ticket image only for the broad ticket proportion, illustrated scene plus information stub, perforation, and tactile print texture; copy no animals, branding, wording, or artwork. The hero ticket must express `公司 → 生活`, `准点发车`, `18:00`, `2026 / 09 / 01`, `LIFE 03`, and `今天认真收工，接下来好好生活`. Let its panorama move from closing a laptop through a station and sunset into dinner, walking, pets, music, and warm home lights. Add collectible variants `晚饭线`, `散步线`, and `兴趣线`. It must feel like a life keepsake, never a prize, coupon, lottery ticket, attendance pass, or permission slip.

## UI map v0.2

Create a coherent `MOLIFE UI 地图 v0.2` using UI map v0.1 as the base, the avatar board as the character system, and the ticket board as the ticket system. Connect five mobile screens with a railway line: `今日站台`, `说说今天`, `AI 已整理`, `今日车票`, and `候车大厅`. Split the result into `6A 今日发车员` with `和两位同事一起带头出发` and `6B 跟上这班车` with `发车员已就位，你也可以一起走`; then converge on `7 下班去生活`. State clearly: `2–3 位发车员带头，所有人都能上车`. Add the secondary `我的旅程` branch with ticket history and life memories. Keep early work screens calm and spacious, add color from the ticket onward, and make the blue-hour life scene the emotional climax. Use mature adult avatars, practical mobile UI, warm textured paper, deep teal type, orange primary actions, and blue secondary actions. Do not add management controls, analytics, rankings, prize language, permission language, purple AI gradients, glossy 3D, or copied characters.

## Avatar system v0.2

Create `Molife 人物头像系统 v0.2` with nine original young working-adult avatars and four expressions for one consistent anchor character. Return to the first user reference's cute spontaneous visual grammar: oversized circular or bean-shaped faces, dot or short-oval eyes, a simple curved mouth, very large ears, exaggerated asymmetrical hair, irregular flat color shapes, and loose handmade rhythm. Suggest adulthood only through collars, knitwear, glasses, headphones, tote straps, and badges. Use warm cream paper, deep teal, yellow, orange-red, leaf green, muted blue, and pink with flat gouache/crayon texture. Do not copy any source character. Avoid photorealism, semi-realistic anatomy, realistic noses or lips, beard stubble, skin shading, 3D, anime, preschool styling, and generic corporate illustration.

## UI map v0.3

Create `MOLIFE UI 地图 v0.3` with the subtitle `总结可选，生活必达`. Use avatar system v0.2 as the authoritative character reference and replace all semi-realistic people with circular faces, dot eyes, tiny mouths, large graphic ears, playful hair blobs, flat collars, and loose cut-paper/gouache proportions. Add a separate `一次设置` branch for `车站发车时间`, showing `工作日默认 18:00`, `今日调整 18:30`, `仅影响设计站`, and `保存时间`. The daily flow contains six screens: `今日站台`, `留下今天（可选）`, `AI 已整理`, `今日车票`, `候车大厅`, and `准点发车`. From the first screen, an orange `直接领票` route bypasses summary and goes directly to the same ticket; a blue `可选总结` route offers `输入文字`, `语音`, and `上传 TXT / MD`, continues through editable AI summary, and rejoins at the identical ticket. All users then share the same lobby and departure flow. State that every ticket has the same chance and that departure leaders only lead everyone in leaving. Keep the ticket beautiful and aspirational, retain `我的旅程`, and include `发车时间只用于组队提醒，不是下班许可`. Avoid summary rewards, skip-state stigma, fixed departure times, realistic people, management approval, surveillance, prize language, and pseudo-text.

## High-fidelity pages 01 — setup and entry

Create `MOLIFE 高保真页面 01 · 设置与入口` as a 16:9 board with three complete mobile screens. Screen one contains `车站发车时间`, `设计站`, `工作日默认 18:00`, `今日调整 18:30`, `仅影响设计站`, and `保存时间`. Screen two contains `设计站 · 今日18:30发车`, `今天想留下点什么吗`, two equal-weight choices `直接领票` and `留下今天（可选）`, plus `不写总结也能参加，每张车票机会相同`. Screen three contains `留下今天（可选）`, tabs `输入文字`, `语音`, `上传 TXT/MD`, a large text field, `让 AI 帮我整理`, and `不总结直接领票`. Use avatar system v0.2 as the only character style, with flat dark-teal phone frames, warm paper, practical tap targets, and no implication that summarizing is required.

## High-fidelity pages 02 — summary and ticket

Create `MOLIFE 高保真页面 02 · 总结与车票` as a 16:9 board with three complete mobile screens. Screen one contains `AI 已整理`, `这是 Molife 帮你整理的今天`, editable cards `完成`, `进展`, `明天`, and equal legitimate exits `确认并领票` and `跳过总结，直接领票`. Screen two presents the collectible ticket with `MOLIFE`, `公司 → 生活`, `设计站 · 18:30 发车`, `2026 / 09 / 01`, `LIFE 03`, and `收好车票`, plus a note that the ticket is identical whether or not a summary was submitted. Screen three contains the lobby countdown `00:24:16`, `已有 12 人上车`, `发车前随机选出 2–3 位今日发车员`, and `每张车票机会相同`; show only avatars and tickets, never summaries. Use flat hand-drawn dark-teal device frames rather than glossy phone hardware.

## High-fidelity pages 03 — departure and journey

Create `MOLIFE 高保真页面 03 · 发车与旅程` as a 16:9 board with three complete mobile screens. The selected-leader screen contains `今日发车员`, `轮到你带头啦`, `和两位同事一起带头出发`, the explanation that leaders only lead everyone in closing laptops, and `一起下班`. The other-member screen contains `跟上这班车`, `今日发车员已就位`, `你也可以一起走`, `一起下班`, and a reminder that departure time is only for grouping. The journey screen contains `我的旅程`, `下班之后，生活继续`, and a chronological collection of tickets and dinner, walking, pet, and music memories. Do not use winner, loser, quota, permission, streak, score, ranking, hours, or KPI language.

## Interaction states 01 — input and AI

Create `MOLIFE 交互状态 01 · 输入与 AI` as a 3 × 2 state board. Include voice recording (`正在听…`, `结束录音`), a selected Markdown file (`今日工作.md`, `24 KB`, `已准备好`), format error (`仅支持 TXT / MD`, `重新选择`), AI processing (`正在收好今天…`, `可以直接领票，不必等待`), AI failure (`这次没整理好`, `再试一次`), and empty input (`今天不想写也没关系`). Every state must retain a visible enabled `直接领票` action. Add `任何异常都不影响领票 · 总结完全可选`. Use the same hand-drawn phone frames and v0.2 avatars; never expose work content or block the game behind AI.

## Interaction states 02 — time and lobby

Create `MOLIFE 交互状态 02 · 时间与候车` as a 3 × 2 state board. Include saved time (`今日发车时间 18:30`, `已保存`), synchronized time change (`今日发车时间已由 18:00 调整为 18:30`, `所有车票与倒计时已同步`), active lobby (`距离发车 00:24:16`, `已有12人上车`), one or two participants (`今天人不多，大家都是发车员`, `一起下班`), three or more participants (`随机选出2–3位今日发车员`, `每张车票机会相同`), and departed (`这班车已准点发出`, `去过更多生活`). Do not use winner, loser, quota, attendance, podium, or permission language.

## Motion storyboard — from finishing work to life

Create `MOLIFE 动效分镜 · 从收工到生活` as four rows of three frames. A: `AI 收好今天`, with the three summary cards gently stacking in `600ms`. B: `车票生成`, with the cards folding/sliding into a perforated `MOLIFE 公司 → 生活` ticket in `900ms`. C: `发车员揭晓`, with avatar windows softly illuminating and three remaining highlighted in `1200ms`, without lottery or elimination cues. D: `准点发车`, with laptops closing, the rail extending into evening, and dinner, walking, pet, and music scenes appearing in `1600ms`. Footer: `轻、慢、温柔，不催促`. Avoid wheels, slots, fireworks, confetti, trophies, or winner effects.
